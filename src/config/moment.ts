import moment from 'moment';
import 'moment/min/locales';
import 'moment/dist/locale/th';

moment.locale('th');

// moment.defineLocale('th-th', {
//   parentLocale: 'th',
// });

export default moment;